# Hive — Development TODO

**Source of truth:** `docs/hive.md` (Portuguese). Every task cites the decisions (`#N`, `D#`) it implements; read them before starting the task.
**Visual reference:** `docs/prototype/` (read `docs/prototype/README.md` first).
**Rules:** `CLAUDE.md`.

## How to use this file

1. Work **top to bottom**, one task at a time. Do not start a task whose dependencies are not checked.
2. Before a task: read the cited decisions in `docs/hive.md`. After a task: all quality gates green (CLAUDE.md), then tick the box and add the branch name next to it.
3. **⏸ Checkpoint** = stop and wait for the human. Do not continue past a checkpoint on your own.
4. If a decision is unclear, missing, or contradicted by reality: stop and ask. Never change a decision yourself.

---

## Stage 0 — Foundation (Rust, WSL side, no UI)

- [x] **0.1 Workspace layout.** `task/0.1-workspace` — *human decisions (2026-09-23, applied in `task/0.1-gates`): `src-tauri/src/main.rs` excluded from coverage and mutants (COVERAGE_EXCLUSIONS.md); the 6 unmaintained-crate advisories pulled in only by Tauri are ignored by ID in `deny.toml`; MPL-2.0 stays allowed.* Cargo workspace at the repo root with members: the existing Tauri crate (`src-tauri`), `crates/hive-protocol` (lib) and `crates/hive` (bin, produces the `hive` binary). Workspace lints: `clippy::unwrap_used` and `clippy::expect_used` denied outside tests. Crates created with `cargo new`, dependencies only with `cargo add -p`. *(#9, #26, #36)*
- [x] **0.2 Protocol crate `hive-protocol`.** `task/0.2-protocol` — *control-frame priority lives in the service writer (0.4); fuzz: `cargo +nightly fuzz run --target x86_64-unknown-linux-gnu decode` from `crates/hive-protocol`.* Frames `[type: u8][channel: u32][length: u32][payload]`, big-endian, explicit max frame size. Control frames carry JSON (serde, versioned enum); terminal frames carry raw bytes. Codec on `tokio-util` + `bytes`. Version handshake (protocol + binary version); mismatch is a hard, explicit error. Control frames have priority over terminal frames. Decoder never panics: typed errors for malformed/oversized frames. Tests: round-trip, partial frames, oversized, unknown type, zero length. `cargo fuzz` target for the decoder. *(#24, #29)*
- [x] **0.3 Internal event model + adapters.** `task/0.3-events` Provider-independent events; adapter trait (raw provider payload in, internal events out, raw payload preserved); Claude Code adapter for hook payloads. No state machine yet (Stage 2). *(#6, #17)*
- [x] **0.4 `hive daemon`.** `task/0.4-daemon` — *fallback runtime dir: `/tmp/hive-<uid>` (checked: owned by the user, mode 0700); killing PTY process groups lands with the PTYs in 0.5.* Unix socket and lockfile with mode `0600` in `$XDG_RUNTIME_DIR` (documented fallback); single instance. Accepts the app connection (through the bridge) and CLI connections. Lifetime tied to the app connection: when it drops, kill every PTY **process group** (graceful signal, then forced after a short grace) and exit. *(#14, #18)*
- [x] **0.5 PTY management.** `task/0.5-pty` — *fish job control gives each job its own process group, so the service ends every process group in the PTY's session (SIGHUP, 2 s grace, SIGKILL); `setsid`/`nohup` escapes survive (risk 9).* Spawn terminals running `fish -C 'set -gx PATH <hive-bin-dir> $PATH'` (never `fish_add_path` without flags) with `HIVE_TERMINAL_ID` in the environment. Input, output, resize, close. One protocol channel per terminal. Pass-through only: no server-side scrollback. No limit on the number of terminals. *(#18, #19, #25, #28)*
- [x] **0.6 `hive bridge`.** `task/0.6-bridge` — *transparent byte relay (the handshake result reaches the app unchanged); a service that fails to start is reported after 5 s with its stderr in `<runtime>/daemon.log`.* stdio ↔ socket. If the socket does not exist, start the daemon detached (`setsid`), guarded by the lockfile. Forward the handshake result. *(#14)*
- [x] **0.7 `hive hook <event>`.** `task/0.7-hook` — *input limit 512 KiB (larger input is replaced by an error marker so the frame stays under 4 MiB); nothing is ever printed on stdout.* Read the hook JSON from stdin with a size limit, attach `HIVE_TERMINAL_ID`, send to the daemon with a ~200 ms internal timeout, **always exit 0**. `--record <file>` appends raw payload + timestamp to a JSONL file. *(#10, #27)*
- [x] **0.8 `hive worktree create | list | remove`.** `task/0.8-worktree` (subagent; merged into `task/0.13-architecture`) — *human decisions (2026-09-23, applied in `task/0.8-followups`): `remove` keeps the `worktree-<name>` branch; hook mode reuses an existing Hive worktree (`claude -w <existing>`); names outside the rule are refused, not normalised; a failed `.worktreeinclude` copy rolls the new worktree back.* Claude convention: `.claude/worktrees/<name>/`, branch `worktree-<name>`. Base branch local **or remote**. Name validation `^[a-z0-9][a-z0-9._-]*$` + reject existing names (identical to the UI dialog). Copy files listed in `.worktreeinclude`. List via `git worktree list --porcelain`. Git always through the `git` executable with separate arguments. Hook mode for `WorktreeCreate` (stdout = **only** the worktree path; any failure = non-zero exit) and `WorktreeRemove`, tested with synthetic payloads but **not registered** in the hooks settings yet (Stage 4). Warn if the project has its own `WorktreeCreate` hook. *(#7, #8, #13, #15, #33, hook details)*
- [x] **0.9 `claude` wrapper.** `task/0.9-claude-wrapper` (subagent; merged into `task/0.11-bench`) — *human-approved (2026-09-23): hook timeout 1 s; a claude started inside a hooked claude (`HIVE_WRAPPED=1`) runs without Hive's settings; empty `PATH` entries are skipped.* The daemon installs a POSIX `sh` script at `<data-dir>/hive/bin/claude`: finds the real `claude` by searching `PATH` without the hive bin dir, recursion guard `HIVE_WRAPPED=1`, then `exec` the real claude with `--settings <hive-hooks.json>`. The daemon also generates `hive-hooks.json` pointing every observation event at `hive hook <event>` by absolute path, with a short hook `timeout`. *(#21, #25, #26, #27)*
- [x] **0.10 Unhooked-claude detection.** `task/0.10-unhooked` — *checks every 1 s for a process named `claude` in the terminal's session; warns (`unhooked_agent` on the terminal channel) after 5 s without `SessionStart`, once per run. An npm-installed claude shows up as `node` and is not detected.* If a `claude` process appears in a terminal's process tree without a `SessionStart` event from that `HIVE_TERMINAL_ID` within a few seconds, emit a warning event. *(#25)*
- [x] **0.11 Benchmarks.** `task/0.11-bench` — *hook latency (300 runs, release): p50 3.8 ms, p99 8.3 ms, max 11.3 ms (`cargo bench -p hive --bench hook_latency`, fails above 20 ms); codec: ~14 GiB/s for 64 KiB terminal frames, ~4.7 M control frames/s (`cargo bench -p hive-protocol`).* Hook latency (process start → event received by the daemon) p99 < 20 ms; codec throughput baseline with `criterion`. *(D3)*
- [ ] ~~**0.12 Hooks spike.**~~ **Moved to 1.12** (human decision, 2026-09-23): passive recording while developing Stage 1, plus one short provoked session. Tooling ready in `task/0.12-spike` (`scripts/spike/`). Original scope, kept for reference: Using `hive hook --record`, run real Claude Code sessions in a scratch repo under `/tmp` (never in this repo, never touching `~/.claude/settings.json`; use `--strict-mcp-config` and the cheapest model) and answer, with evidence:
  1. Does a `WorktreeCreate` fired by a subagent carry `agent_id` / `agent_type`?
  2. Does Claude Code keep writing to the terminal (spinner) during a long tool call? Measure output gaps.
  3. Does the "subagent + WorktreeCreate hook → isolation error" bug reproduce?
  4. Does Claude Code refuse to edit a file changed externally since its last read?
  5. Confirm `Stop` does not fire on Esc; what does?
  6. What happens to a subagent's worktree and branch when it finishes with changes?

  Write `docs/spike/stage-0.md`: answers, evidence, catalog of hook events and fields, and **proposed changes to `docs/hive.md`** (as table rows, in Portuguese) — do not apply them. *(Pontos em aberto #7)*
- [x] **0.13 `docs/architecture.md`.** `task/0.13-architecture` — *to be updated with the spike findings (0.12).* Contracts, module map, message catalog, sequences (bridge start, handshake, terminal open/close, hook event, app disconnect), how to run and test.

**⏸ Checkpoint 0** — the human reviews Stage 0 and updates `docs/hive.md` (the spike report comes at 1.12, before checkpoint 1; "Pontos em aberto" #7 in `docs/hive.md` still says "spike da Etapa 0" — the human updates it).

---

## Stage 1 — Terminal + one agent (Tauri app)

- [x] **1.0 ⚠ Ask the human first: how the Windows app is built and run during development.** `task/1.0-windows-dev` — *human decision (2026-09-23): build in WSL with `cargo xwin` (msvc target, static CRT), Vite in WSL, run via `scripts/win-dev.sh` (see `docs/architecture.md`).* The code lives in WSL, but the app is a Windows app (WebView2) that talks to WSL through `wsl.exe hive bridge`. Do not guess; wait for the decision. *(Pendente para a Etapa 1)*
- [x] **1.1 App shell per the prototype.** `task/1.1-app-shell` — *`docs/prototype/README.md` is missing, so tokens and glossary are a proposal in `docs/ui-reference.md` (pending human approval). Regions in `src/shell/`, tokens in `src/styles.css`, fonts via `@fontsource`. Window has no native decorations; the title bar drags it and holds the window buttons. Sidebar resize, breadcrumb and "N active agents" deferred to the tasks that feed them. `bun run e2e` (Playwright, `e2e/`) needs `libnss3`/`libnspr4` on the system.* Title bar, sidebar, terminal area, right panel placeholder, status bar. Design tokens from `docs/prototype/README.md`. IBM Plex Sans/Mono **bundled locally** (no Google Fonts). Dark theme. All UI text in English using the glossary in `docs/prototype/README.md`. *(#23, #32, #34)*
- [x] **1.2 Frontend foundations.** `task/1.2-frontend-foundations` — *done before 1.1. Zustand store in `src/store.ts` (service data only through `apply(message)`, `useAgent(id)` selector); React Compiler via Babel (`@rolldown/plugin-babel`); Biome lint; `bun test` collects only `src/` (Playwright goes in `e2e/`); template `greet` removed. TanStack Virtual not added yet: add it with the first long list. Bun coverage only counts files some test imports.* React + React Compiler. Agent state in an external store with per-agent subscriptions (`useSyncExternalStore` or Zustand selectors), fed only by service events: no domain logic in TypeScript. TanStack Virtual for long lists. **No router library**: which view, panel or dialog is open is UI state in the store (#38). `bun test` with `happy-dom` and a 100% line coverage threshold in `bunfig.toml`. *(#30, #37, #38, D2)*
- [x] **1.3 Transport.** `task/1.3-transport` — *`src-tauri/src/lib.rs`: `wsl.exe [-d $HIVE_WSL_DISTRO] --exec /bin/sh -c 'exec "${1:-$HOME/.cargo/bin/hive}" bridge' sh [$HIVE_BRIDGE]` (no login shell, so no fish config; `win-dev.sh` points `HIVE_BRIDGE` at `target/debug/hive` through `WSLENV`). Control messages go to the UI on one `Channel`, terminal bytes on one `Channel` per terminal; Rust allocates terminal channels. `disconnected {reason}` (bridge stderr) added to the store. A reloaded UI re-attaches: old terminals closed, `welcome` replayed. TS `Transport` in `src/transport/` (Tauri + mock; mock outside Tauri or with `?mock`). App and `hive` must share one version number (handshake).* Rust side of the app spawns `wsl.exe hive bridge` and speaks the frame protocol. One Tauri `Channel` per terminal (no Tauri events for terminal bytes). A mock transport so the UI runs in a browser and can be tested with Playwright. *(#14, #24)*
- [x] **1.4 Connection status.** `task/1.4-connection-status` — *`welcome` gained an optional `distro` (the service's `WSL_DISTRO_NAME`; `PROTOCOL_VERSION` stays 1); the app side adds `app_version`/`app_protocol` to `version_mismatch`. `src/shell/ConnectionBlock.tsx` makes the workspace `inert` under a modal on mismatch or disconnect (title bar stays usable) with a "Reconnect" that calls `connect` again. The fix text includes `pkill -f 'hive daemon'` because a refused handshake leaves the old service running. Mock: `?mock=mismatch`, `?mock=disconnected`.* Status bar shows the WSL distribution and connection state; a version mismatch blocks with a clear message and how to fix it. *(#29)*
- [x] **1.5 Projects.** `task/1.5-projects` — *orchestrator defaults, pending human review at checkpoint 1: the service owns the list in `<data>/hive/projects.json` (atomic, 0600; a corrupt file is moved to `projects.json.corrupt` and the list starts empty); `add_project` takes a typed WSL path (no Windows picker), refuses with a typed `error` + `message`, normalises to the main worktree (a subfolder or linked worktree adds its repository) and treats a duplicate as a no-op that answers the same project; ids and names come from the service (Claude worktree → folder name, else branch). The app asks `list_projects` after every `welcome`; a "Refresh worktrees" button in the sidebar header (not in the prototype) asks again; no file watching until Stage 3. The add-project dialog is new (the prototype has none) and uses a native `<dialog>`. No remove-project (not in the prototype), no tree arrow keys yet (1.9), no TanStack Virtual yet. `PROTOCOL_VERSION` stays 1 (new messages only; binary versions must match anyway). Mock: `?mock=empty`.* Add project (a folder inside WSL), list its worktrees, empty state (screen 1e).
- [x] **1.6 New worktree dialog** (screens 1c/1d). `task/1.6-new-worktree` — *orchestrator defaults, pending human review at checkpoint 1: the service calls `hive::worktree` directly (no subprocess). New messages `list_branches`/`branches`, `validate_worktree_name`/`worktree_name_validated` (sent on every keystroke; `check_name` = rule + existing folder, no git; answers matched by name since they can arrive out of order), `create_worktree`/`worktree_created`/`create_worktree_failed`. Error texts are the CLI's, shown as is. `create` now returns notes (competing `WorktreeCreate` hook, "copied N files listed in .worktreeinclude"); the CLI prints them as `hive: <note>` on stderr (the include count is new CLI output); the dialog stays open to show them, otherwise it closes. "default" badge = the branch checked out in the main worktree. The preview's folder/branch come from the service. A pick hidden by the filter falls back to the first branch shown (no persistence as in the prototype). The terminal opens at 80×24 with output dropped until 1.7. Branch list virtualized (`@tanstack/react-virtual`), names capped at 1 MiB. The "New worktree" chip shows on row hover/focus. Fixed a test leak: `main.test` now unmounts its root.* Same name validation as the CLI; base branch from local and remote branches with a filter; "Open a terminal in the new worktree". Calls `hive worktree create`. *(#33)*
- [x] **1.7 Embedded terminals.** `task/1.7-terminals` — *orchestrator defaults, pending human review at checkpoint 1: `src/terminals.ts` owns xterm.js (6.0) outside React; the Terminal is created before `openTerminal` so no output is lost. Tabs in the store (`tabs`, `activeTab`); title = worktree name from the service (project name added for duplicates), "exited" and "no hooks" badges (no prototype element for either). Only the shown terminal is opened, fitted and gets WebGL; hidden ones drop it; context loss → DOM renderer. `scrollback` default 5000 in the store, not persisted (settings persistence undecided). "+" opens a terminal in the selected worktree. `interceptKeys` is the hook for 1.9. No `onBinary` (only legacy X10 mouse reports use it), so no transport change. Clipboard via `navigator.clipboard`: not yet checked inside WebView2. Closed tabs keep their tiny `terminals` record.* xterm.js managed **outside** React; tabs; WebGL renderer only on visible terminals; bounded, configurable scrollback; Ctrl+Shift+C / Ctrl+Shift+V copy and paste. *(#18, #28)*
- [x] **1.8 Agent detection.** `task/1.8-agent-detection` — *orchestrator defaults, pending human review at checkpoint 1: the service keeps agents by `session_id` (presence only) and places them by the payload `cwd` in the deepest followed worktree (`projects::place`). New `agent_detected {id, project, worktree, cwd}` / `agent_removed {id}` on the terminal's channel (removed on `SessionEnd` or terminal exit, before `terminal_exited`). Subagent events ignored. Agents outside every followed project are sent with null ids and not shown; placement is done once (a project followed later does not move them). Raw `agent` forwarding kept (tests, latency bench). Sidebar row "Claude" with the idle icon; click → `activateTab`. Mock: `cd <dir>` + `claude`.* A `SessionStart` event from a terminal shows the agent under its worktree (position by the payload's `cwd`, link to the tab by `HIVE_TERMINAL_ID`). *(#19)*
- [x] **1.9 Shortcuts.** `task/1.9-shortcuts` — *orchestrator defaults, pending human review at checkpoint 1: one window `keydown` listener (`src/shortcuts.ts`); a focused terminal keeps only the shortcuts from xterm through `interceptKeys`, so they bubble to the window and run once. Nothing runs under the connection block or while a dialog is open. Ctrl+Shift+N: selected project → project of the selected worktree → first project → add project when there is none. F8 (no modifiers) is bound to `nextPending`, a no-op until pending states exist (2.3). The worktree picker follows the prototype (560px, 44px from the top, transparent backdrop, click outside closes), paths shown as the service sends them (no `~`). "+" still opens a terminal in the selected worktree (the prototype's "+" opens the picker). Tree: ↑/↓ between rows, ←/→ collapse/expand a project, Enter selects (no Enter-focuses-terminal as in the prototype).* Ctrl+Shift+T (worktree picker → new terminal), Ctrl+Shift+N (new worktree), Ctrl+Shift+B (files panel), Ctrl+Shift+O (add project), F8 (next pending). Every other key goes to the terminal untouched. *(#35)*
- [x] **1.10 App lifetime.** `task/1.10-app-lifetime` — *orchestrator defaults, pending human review at checkpoint 1: agent states do not exist yet, so closing asks whenever **any detected agent** exists (`agentsAtRisk` in `src/shell/CloseAppDialog.tsx`, the one function Stage 2 narrows to working / waiting for permission / waiting for you). Title bar Close, Alt+F4 and the taskbar go through one Tauri close request (`guardClose` with `onCloseRequested`); a confirmed close calls `destroy` (new capability `core:window:allow-destroy`). Dialog is new (no prototype screen): "Close Hive?", "N agents are running. Closing Hive ends every terminal and the agents in them.", Cancel · Esc / Close Hive · Enter (focused). On `RunEvent::Exit` the app closes the bridge stdin and waits up to 2 s, then kills the bridge. The service side (app connection drop ends every PTY process group) was already proven by the Stage 0 tests.* Closing the app ends every terminal and agent; confirmation dialog if any agent is working, waiting for permission or waiting for you. *(#18)*
- [x] **1.11 Load test.** `task/1.11-load-test` — *passes on Chromium/Linux with the Windows GPU through WSL's D3D12 on an idle machine (input p95 ~37 ms, 60 fps, no long task, nothing dropped); fails under heavy CPU load and with software GL. Generated Claude Code-like recording (`src/transport/replay.ts`); real asciinema casts accepted via `HIVE_LOAD_CAST`. Not yet measured in WebView2; hidden terminals waste main-thread time in xterm's DOM fallback (see `docs/architecture.md`, Load test).* 20 terminals replaying recorded Claude Code output at the same time; the focused terminal must stay fluid. If it fails, stop and report: the planned fallback (headless terminal emulator in the service) is a human decision. *(#28)*

- [ ] **1.12 Hooks spike report (moved from 0.12).** Throughout Stage 1 the human runs Claude Code in this repo through `scripts/spike/dogfood.sh` (records in `target/spike/`), and once runs the provoked session in `scripts/spike/README.md` (Part 2, `/tmp/hive-spike`). Answer the six questions of 0.12 with evidence and write `docs/spike/stage-0.md` (answers, evidence, catalog of hook events and fields, proposed changes to `docs/hive.md` as table rows in Portuguese — do not apply them). Ask the human when the recordings are ready; never run the real `claude` yourself. *(Pontos em aberto #7)*

**⏸ Checkpoint 1**

---

## Stage 2 — Agent states

- [x] **2.1 State machine in the Rust service** `task/2.1-state-machine` — *built without the hooks spike (human decision, 2026-09-23); defaults pending human review at checkpoint 2: `agent_state {id, state, subagents}` on the agent's terminal channel, sent when it changes, after `agent_detected`, as `ended` before `agent_removed`, and for every live agent after `welcome`. Pure logic in `hive::states` (the clock is passed in, as in `hive::watch`). A subagent joins on its first event with a state (not only `SubagentStart`) and leaves on `SubagentStop`; at most 32 per agent, ids/types over 256 bytes ignored. Rule 2 (5 s, `states::SILENCE`, checked every 1 s) counts from the later of the last output and the last hook event, and also moves subagents in working / waiting for permission to waiting for you. As written, rule 2 also turns a permission prompt left unanswered for 5 s into waiting for you, unless Claude keeps drawing while it waits (spike question 2).* (the UI only renders the resulting state) from the mapping table in `docs/hive.md` ("Mapeamento de estados"), including `Notification` by `notification_type`, "the most urgent state wins", and the PTY-silence reconciliation for interrupts.
- [x] **2.2 Sidebar tree** `task/2.2-sidebar-states` — *orchestrator defaults, pending human review at checkpoint 2: `agent_state` is stored as sent in `agentStates` (by session id, apart from `agents`, so either may arrive first); an agent with no state yet shows as idle. Agent and subagent rows follow the prototype (icon, title over the state name, alerting states colored; subagents at 56px with tree lines, "subagent: <agent_type>", "unknown" when the type is null); clicking a subagent shows its agent's terminal. No "time since" (the protocol has no timestamp). The working pulse stops under `prefers-reduced-motion`. The close confirmation counts only working / waiting for permission / waiting for you (#18). `?mock=states` shows every state.* Project → Worktree → Agent → Subagents, with the state icon (color + shape, from `StateIcon.dc.html`).
- [x] **2.3 Propagation and pending counter.** `task/2.3-propagation` — *orchestrator defaults, pending human review at checkpoint 2: the service adds `urgency` (0 ended … 6 waiting for permission) and `pending` to `agent_state` (PROTOCOL_VERSION stays 1); pending = waiting for permission, error, waiting for you (the prototype and the glossary count only the high-urgency ones). A collapsed project or worktree shows the state icon of highest `urgency` inside (the prototype shows a bell for alerting states and a bell on expanded projects too; not done). Worktrees with agents collapse (keyed `worktree:<id>`, since a main worktree shares its project's id). "N pending" is the chip in the sidebar bar (click = F8), agents outside every project included. F8: next pending agent after the selected one (else the shown terminal's) in tree order, wrapping; expands, selects, shows its tab when it has one, scrolls into view. No Shift+F8 (#35 names none).* Collapsed nodes show the most urgent state inside; "N pending" counter; F8 jumps to the next pending agent.
- [x] **2.4 Notifications.** `task/2.4-notifications` — *orchestrator defaults, pending human review at checkpoint 2: transitions are detected in the frontend by comparing the stored state with the new `agent_state` (`src/notify.ts`); an agent's first state and the snapshot after `welcome`/reconnect are silent. The tone is an 880 Hz, 0.2 s Web Audio beep, one per 500 ms however many agents change. "Finished" = working or with subagents → waiting for you, as an OS notification through `tauri-plugin-notification` ("Agent finished", "project · worktree: waiting for you"). Always on: no "only when unfocused" and no mute setting. Nothing is shown outside Tauri.* Sound only when entering waiting-for-permission, waiting-for-you or error; OS notification when an agent finishes.
- Checkpoint 2 follow-ups `task/2.5-checkpoint-2-fixes` — *closing also asks when an agent is with subagents (#18). The "Add project" error showing `/` beside an empty field was not an empty submit (the Add button is disabled when the field is empty, and the service answers an empty path with `not_absolute`): the refusal of an earlier `/` stays shown after the field is cleared. Human decision, `task/2.6-add-project-error`: editing the field clears the refusal; Add stays disabled for a blank or whitespace-only field, and the service refuses a blank path with `empty_path` ("Enter a folder") before the absolute-path check.*

**⏸ Checkpoint 2**

---

## Stage 3 — See what the agent did

- [x] **3.1 File watching** `task/3.1-file-watching` — *orchestrator defaults, pending human review at checkpoint 3: `watch_worktree {path}` / `unwatch_worktree` / `files {path, files, truncated}` on channel 0, one watched worktree at a time (the files panel's: the selected worktree, a project's main one, or the selected agent's; re-sent after a new `welcome`). Files = `git ls-files --cached --others --exclude-standard` (not-UTF-8 names and nested repositories skipped, cap 50 000 files / 3 MiB of names). inotify through `nix` on the listed directories, untracked (even empty) ones and the git dir's `HEAD`/`index`, at most 8192 watches, never in ignored trees; any event (overflow included) → full re-list after 200 ms quiet (1 s at most); `files` only when the list changed. Every Hive git call now runs with `GIT_OPTIONAL_LOCKS=0`. Wired with 3.2: `State::worktree_changed` (`hive::daemon`) also pushes `changes` after every refresh, and "All" shows `files` with the changes' statuses (deleted files kept, a cut list says so). Mock: `touch <name>` in a worktree's terminal.* In the service (inotify) → real-time file tree.
- [x] **3.2 Git diff** per worktree; right panel (screen 1g), toggled by Ctrl+Shift+B. `task/3.2-git-diff-panel` — *orchestrator defaults, pending human review at checkpoint 3: diff base = `HEAD` (staged + unstaged + untracked, as `git status`; the empty tree before the first commit), not the merge-base with a base branch. `list_changes {path}` → `changes {path, files, added, removed, error}` (`hive::changes`): `git status --porcelain=v2 -z` + `git diff --numstat -z`, renames detected, git output capped at 16 MiB, untracked files counted here (≤ 8 MiB, NUL in the first 8000 bytes = binary = null counts), at most 3 MiB of files per message (the rest named in `error`, totals always complete). Untracked shows as "A", renamed as "R" in blue (the prototype has only M/A/D); a folder takes the strongest status inside (D > A > R > M) and shows a dot when collapsed. The panel shows the selected worktree, a selected project's main worktree, the selected agent's worktree, else the shown terminal's; it asks again when that worktree changes (no live refresh until 3.1's watcher pushes `changes`). Tree virtualized, one focusable element (↑/↓, ←/→, Enter). Selecting a file sets `openFile {worktree, path}`; its header shows under the tree, the viewer/diff slot (`FileView`) is empty until 3.3. "All" shows the changed files too until 3.1's file list exists. No panel resize (prototype has it).*
- [x] **3.3 Viewer and diff** with CodeMirror 6 and `@codemirror/merge` (diff read-only). *(#31)* `task/3.3-viewer-diff` — *orchestrator defaults, pending human review at checkpoint 3: `open_file {worktree, path}` → `file {worktree, path, content, base, version, binary, too_large, error}` (`hive::file`); the path must be relative, without `.`/`..`, at most 4096 bytes, and resolve (symlinks followed) inside the worktree to a regular file; `base` from `git ls-tree`/`git cat-file` at `HEAD` (a staged rename's old path); each side capped at 1 MiB (not 2 MiB: two sides must fit one 4 MiB frame, and an answer whose JSON would still not fit is sent as too large); not UTF-8 counts as binary, like a NUL in the first 8000 bytes; `version` = byte length + 64-bit SipHash of the bytes on disk. The app asks again on each new `changes` of that worktree and after `welcome`. A file among the changes shows as a read-only `unifiedMergeView` (unchanged stretches collapsed), any other file as read-only text under "No changes in this file."; One Dark (`@codemirror/theme-one-dark`) with the prototype's surfaces and diff colors; languages from `@codemirror/language-data`, loaded on demand. The deleted chunk has no line numbers (CodeMirror's unified view), unlike the prototype's two number columns.*
- [x] **3.4 Selection → terminal.** Selecting code writes only the reference into the active terminal, e.g. `@src/checkout/validators.ts (lines 44–46)`. `task/3.4-selection-reference` — *orchestrator defaults, pending human review at checkpoint 3: Ctrl+Shift+L (from anywhere, #35 style) or a terminal icon button in the file view's header writes `@<path> (lines a–b) ` / `(line a) ` (trailing space, no Enter) into the shown terminal and focuses it; the path is the open file's worktree-relative path, double-quoted when it has whitespace (`@"docs/my notes.md"`, an assumption for the 1.12 spike). Lines are the main selection's, new-file numbers; a selection ending at a line's start leaves that line out. Removed lines of the diff are CodeMirror widgets, not selectable text, so they can never be referenced. Disabled (the tooltip says why) with no selection, no terminal, an exited one, or when the terminal is in another worktree: the agent's worktree as the service placed it, else the worktree the tab opened in (no new service field). A new answer for the open file resets the selection, as the view state is rebuilt.*
- [x] **3.5 Editing (3b).** Save through the service (temp file + rename) with a version check; auto-reload when the buffer is clean; conflict banner when the buffer is dirty (reload / keep mine / view diff); "agent working here" badge; "open in external editor". No LSP. *(#31)* `task/3.5-editing` — *orchestrator defaults, pending human review at checkpoint 3: `save_file {worktree, path, content, version}` → `file_saved {version}` / `save_failed {error: conflict | too_large | invalid_path | io, message}` (`hive::file::save`): `open_file`'s path rules, 1 MiB cap, temporary file in the same folder (0600, then the file's mode; 0644 when new), fsync, version checked right before the rename (a write in between is lost: known race), `version` null creates a missing file. A file without changes opens editable; a changed one opens as its read-only diff with Edit / Diff. Ctrl+S only inside the editor (a CodeMirror key, not an app shortcut) and a Save button; one save at a time; a clean buffer reloads on a new version, a dirty one shows "Changed on disk." with Reload / Keep mine / View diff (read-only diff of the edits against the disk); lines split on "\n" only, so CRLF files save unchanged. Leaving a file with unsaved edits asks (`window.confirm`); closing the app does not. "Agent working here" = an agent (or a subagent in its own worktree) placed there that is working, with subagents or waiting for permission. "Open in external editor": `open_in_editor` → `editor_target {windows_path, error}` from `wslpath -w`, opened by `@tauri-apps/plugin-opener` `openPath` with the Windows default app (capability `opener:allow-open-path`, any path; unverified on Windows); files Windows would run (`.bat`, `.cmd`, `.exe`, `.js`, `.lnk`, `.ps1`, …) refused; the browser mock only says so. Added `@codemirror/commands` (undo, standard keys, Tab). Mock: `write <path> <text>` in a worktree's terminal.*

**⏸ Checkpoint 3**

---

## Stage 4 — Subagents and worktrees

- [x] **4.1** Register `WorktreeCreate` / `WorktreeRemove` in the generated hooks settings. *(#15)* `task/4.1-worktree-hooks` — *built without the hooks spike; defaults pending human review: timeouts 60 s (create) and 10 s (remove); after succeeding, `hook-create`/`hook-remove` forward the call like `hive hook` (200 ms, `HIVE_TERMINAL_ID`; create adds `worktree_path`), never changing stdout or the exit code; new events `worktree_created {name, path}` / `worktree_removed {path}` keep `agent_id`/`agent_type` for 4.2; the service then sends an unasked `projects`. Payload fields (`name`, `cwd`, `worktree_path`, `agent_id`) to confirm by spike 1.12.*
- [x] **4.2** Link subagents to their parent (using what the spike found); a subagent's own worktree appears nested under it, not at project level. *(#22)* `task/4.2-subagent-worktrees` — *built without the hooks spike; defaults pending human review: parent linking is 2.1's (`agent_id` under the `session_id`); `agent_state.subagents[]` gains `worktree` (the worktree's id, protocol stays 1), linked by a `WorktreeCreate` carrying the subagent's `agent_id`, else by the subagent's own `cwd` lying in a worktree other than its agent's (placed once per new cwd, only when the agent itself was placed); unlinked by that path's `WorktreeRemove` or the subagent leaving. The sidebar shows it under the subagent (prototype row, out of the arrow keys) and hides it at project level unless an agent runs there. To confirm by spike 1.12: `agent_id` in `WorktreeCreate`, a subagent's `cwd` being its worktree.*
- [x] **4.3** Worktrees removed by `WorktreeRemove` disappear from the sidebar. `task/4.3-removed-worktrees` — *defaults pending human review: the removal itself already worked through 4.1's `projects` push; the service now also leaves out prunable worktrees (directory gone, still in `git worktree list`). In the store a vanished selected worktree falls back to its project (nothing if the project went too) and its `worktree:<id>` collapsed key is dropped; its terminal tabs stay open (a tab re-selects the gone path on click). A live agent placed in it is not re-placed: like an agent outside every project it leaves the tree but still counts as pending and F8 reaches its tab. Mock: `worktree-remove <name>` in a terminal.*
- [x] **4.4 Worktree context menu** (human request, 2026-09-24; not in `docs/hive.md` or the prototype). `task/4.4-worktree-menu` — *human decisions: rename moves the folder **and** the `worktree-<name>` branch; delete always asks, and when refused (changes, or a process working there) shows why and offers "Delete anyway" (`--force`), keeping the branch; the menu also has "New terminal here", "Copy path", "Open in Explorer". Defaults pending human review: `remove_worktree {path, force}` → `worktree_removed {project, path}` / `remove_worktree_failed {path, message}`; `rename_worktree {path, name}` → `worktree_renamed {project, from, path}` / `rename_worktree_failed {path, name, message}` (`Projects::remove_worktree`/`rename_worktree`, protocol stays 1). Only linked worktrees of followed projects; only Claude ones (`.claude/worktrees/`) are renamed, and their branch only while still on `worktree-<old>` (a failed branch rename moves the folder back). "In use" = any process whose `/proc/<pid>/cwd` is inside the worktree (at most 5 named); it blocks rename always and delete unless forced. Terminal tabs of a deleted worktree stay open (as 4.3). "Open in Explorer" is `open_in_editor` with an empty path (the folder's `wslpath -w`), opened by `openPath`; its errors and "Copied <path>" show in the status bar (new `notice`, click to dismiss). Menu opens on right click or the menu key, arrows move, Esc/Tab/click outside/scroll/blur close it. Not checked in a real browser: this WSL lacks `libnspr4` for Playwright's Chromium.*
- [x] **4.5 Phosphor icons** (human request, 2026-09-24). `task/4.5-phosphor-icons` — *added `@phosphor-icons/react` (MIT, no dependencies; ~5 kB gzip after tree-shaking). Agent/subagent states in the sidebar (and collapsed roll-ups) are outlined Phosphor icons (bold, 14 px) in the same `--state-*` colors, no filled circles: idle `CheckCircle`, working `CircleDashed` spinning (still under reduced motion), waiting for permission `CircleHalf` (human's pick over the warning triangle), waiting for you `ChatCircleDots`, error `XCircle`, running subagents `CirclesThree`, ended `StopCircle` (these last four are the agent's picks, pending human review). Every close X (dialogs, tabs, file panel, window) is Phosphor's `X`. Other icons still come from the prototype's inline SVGs. `e2e/states.e2e.ts` updated (14 px, spin) but not run: this WSL lacks `libnspr4` for Playwright's Chromium.*
- [x] **4.6 Right panel toggle icon** (human request, 2026-09-24). `task/4.6-panel-icon` — *the terminal bar's files-panel toggle is Phosphor's `SidebarSimple`, mirrored (panel on the right); the panel's own collapse button was already Phosphor's `X` (4.5).*
- [x] **4.7 Tabs per worktree; claude on a new worktree** (human request, 2026-09-24). `task/4.7-tabs-per-worktree` — *human decisions: the tab bar shows only the selected worktree's tabs (a selected project shows its main worktree's; nothing selected shows all); the new-worktree dialog gets "Start claude in the terminal", ticked by default, which types `claude` + Enter into the new terminal. **This departs from `docs/hive.md` ("não inicia… os agentes", line 39): the human updates `docs/hive.md` (and the first paragraph of `CLAUDE.md`).** Defaults pending human review: a tab belongs to the worktree it opened in (`tabPlace`); once that worktree is gone, to the project whose folder holds it; a selected agent shows its terminal's worktree; switching keeps the shown terminal when it belongs there, else shows that place's last tab (no per-worktree memory of the last shown tab); a closed tab hands over to its neighbour among the shown ones; the open file's tab goes with its worktree; a worktree without terminals shows "No terminal in <name>" with a New terminal button, centered in the terminal area under Phosphor's `TerminalWindow` (32 px). Hidden tabs keep running and are reached through the tree, an agent row or F8. E2E specs updated (terminals, new-worktree) but not run: this WSL lacks `libnspr4` for Playwright's Chromium.*
- [x] **4.8 New chat from a worktree row** (human request, 2026-09-24). `task/4.8-new-chat-button` — *hovering (or focusing) a worktree row shows a Phosphor "+" that opens a new "chat": a terminal in that worktree with `claude` typed into it (`openClaude`, shared with 4.7's dialog option). Same departure from `docs/hive.md` line 39 as 4.7. The main worktree has it too.*
- [x] **4.9 Files view; Changes panel** (human request, 2026-09-24, as in Orca). `task/4.9-files-sidebar` — *first built as left-sidebar tabs (Worktrees | Files) with Changes on the right; **superseded by the human in 4.11**: the left sidebar is only the worktree/agent tree again, and the right panel (Ctrl+Shift+B, "Side panel") has the tabs Files | Changes | Sessions for the shown worktree (`panelWorktree`). Files shows every file with the changes' statuses; Changes the changed files with their totals (the old All/Diff switch and `changedOnly` are gone). The service watches the shown worktree while the panel is open.*
- [x] **4.10 File search** (human request, 2026-09-24, as in Orca and other IDEs). `task/4.10-file-search` — *the sidebar's Files gets "Find files" with Names | Contents. Defaults pending human review: Names filters the shown worktree's listed and changed files by path (any case, first 500 shown) in the app; Contents asks the service `search_files {worktree, query}` → `search_results {worktree, query, matches[{path, line, text}], truncated, error}` 250 ms after typing pauses (`hive::search`: `git grep -n -I -i -F -z --untracked`, so tracked and not-ignored untracked files, binary skipped, fixed string, any case; query 1–256 bytes on one line; at most 50 lines per file and 1000 matches, lines cut at 300 characters); results grouped by file with the text marked; a result opens its file (as the tree does) with the line selected and centered (`gotoLine`, `revealLine`). No regex, case or whole-word options; no replace. Mutation testing found that a helper mutant could loop forever allocating and take down the WSL VM: `search::parse` now bounds its loop by the input's length. Gates here run with `CARGO_BUILD_JOBS` ≤ 3 and one mutant at a time (the VM has 10 GB).*
- [x] **4.11 Claude session history** (human request, 2026-09-24, as in Orca). `task/4.11-sessions` — *human decisions: done now (it was Fase 2 in `docs/hive.md`, lines 82/95/108) for the followed projects' sessions, including ones run outside Hive (departs from #20); the human updates `docs/hive.md`. Shown in the right panel's Sessions tab, only the shown worktree's sessions (no scope switch), with a search; each has Orca's menu: Resume in Worktree (shows its terminal when it already runs), Continue in New Session (`--fork-session`), Copy Resume Command, Open Log, Reveal Log, Open Working Directory, Copy Session ID, Copy Log Path, Delete (asks first; refused while running). Defaults pending human review: the service reads `$CLAUDE_CONFIG_DIR/projects` or `~/.claude/projects` (`hive::sessions`), only folders whose name matches a followed project's path (non-alphanumerics as `-`), and places each `<id>.jsonl` by its first `cwd` in the deepest followed worktree; a summary (title: custom, else Claude's `ai-title`, else the first prompt; last message with text; count of user/assistant messages with text; last model and branch) is cached per log until its time or size changes; at most 64 MiB read per log, 300 characters per text. `list_sessions` → `sessions {sessions, error}` when the tab opens or on Refresh (no live refresh); `locate_session {id, target: log|folder}` → `session_located` (`wslpath -w`), opened by the opener plugin (`openPath`, or `revealItemInDir` for Reveal Log); `delete_session {id}` removes the log and the `<id>/` folder beside it (subagents' logs; a symlink is left alone). A click on a session resumes it. Resuming types `claude --resume <id>` into a new terminal in the session's cwd (Hive starts agents here too). `tabPlace` now picks the deepest worktree holding a path. E2E not added: this WSL lacks `libnspr4`.*
- [x] **4.12 Session states; sessions back after a restart; "Diff" tab** (human request, 2026-09-24). `task/4.12-session-states-restore` — *human decisions: every session of the history shows a state with the existing icons, sessions running outside Hive included; the sessions running in Hive when the app closes come back when it opens; the right panel's "Changes" tab is named "Diff". Defaults pending human review: a session in a Hive terminal shows its live state; any other gets `state` from how its log ends (`sessions::Ending`, read in the same pass as the summary: an assistant `end_turn`/`stop_sequence` = turn done, other assistant records and prompts/tool results = working, `[Request interrupted by user…` = interrupted, `isApiErrorMessage` = error, a user `<…>` tag = turn done) and `running`: each `claude` process (`/proc`, once per terminal session) runs the newest session of its cwd. Running: working / waiting for you (turn done or interrupted) / error / idle (nothing yet); stopped: ended (turn done) / waiting for you (left mid-turn) / error. The Sessions tab asks again every 5 s while open. A session running outside Hive is neither resumed nor deleted from Hive (a fork is allowed). On shutdown the service writes the Hive terminals' sessions with a cwd, in terminal order, to `<data>/hive/open-sessions.json` (0600) before ending the terminals; the next daemon reads and removes it and sends `restore_sessions {sessions[{id, cwd}]}` once, after the first app's snapshot; the app resumes each with `claude --resume <id>` in a new terminal. Ids that are not session ids and relative folders are dropped (the id is typed into a shell). Plain terminals without claude are not restored. The ⋯ button of a session is now bordered and brighter.*
- [x] **4.13 Agent tabs** (human request, 2026-09-24, as in Orca). `task/4.13-agent-tabs` — *human decisions: a terminal tab no longer shows its project (a tab bar shows one worktree's tabs); a tab running a Claude agent shows the agent's state icon and the session's name given by Claude; the close × is bigger. Defaults pending human review: the service reads the name (`/rename`'s custom title, else Claude's `ai-title`; not the first prompt) from the session log in Claude's folder for the agent's cwd (`Sessions::title`, cached like the history) when the agent appears, after every event while it has none, and at the end of every turn; it sends `agent_title {id, title}` on the agent's channel when it changes, and again for every named agent after `welcome`. Until named, the tab shows the worktree's name. Names are cut at 220 px; the tooltip has the name and the folder. The close × is Phosphor's `X` at 15 px (was 12) in a 22 px button.*
- [x] **4.14 Pending bell in the title bar** (human request, 2026-09-24). `task/4.14-pending-bell` — *human decision: the sidebar's "N pending F8" counter moves to the title bar as only a bell with a number (departs from `docs/hive.md` line 75's counter "no topo"; the human updates it). Defaults: Phosphor `Bell`, filled and in the waiting-for-permission color with a count badge when some agent is pending, outlined and disabled otherwise ("Nothing pending"); a click is F8; the sidebar's bar keeps Refresh and + Project.*
- [x] **4.15 Resizable side panels** (human request, 2026-09-24). `task/4.15-resizable-sidebars` — *human decisions: both side panels resize within a minimum and a maximum; dragging the right panel past its minimum closes it. Defaults pending human review: left sidebar 200–480 px (264 by default), right panel 280–640 px (380 by default); the right panel closes when dragged under 200 px and reopens at its last width; a 6 px handle on each inner edge (a focusable `separator`, ←/→ move it 16 px); the widths are UI preferences kept in the window's `localStorage` (`hive.widths`), not in the service. Also: the pending bell's count badge is orange (`--state-you`) with a ring of the bar's color, set apart from the yellow bell.*
- [x] **4.16 Title bar logo** (human request, 2026-09-24). `task/4.16-titlebar-logo` — *the title bar's Hive logo follows the human's drawing: six hollow hexagons and one solid (top right), in the color of the "Hive" text beside it (`currentColor`, `--text-2`), after trying the logo's yellow and the system's blues (human decisions), still 16 px. The app icons (`src-tauri/icons/`) are unchanged.*

- [x] **4.17 Release pipeline** (human request, 2026-09-24: distribution through GitHub Releases). `task/4.17-release` — *`.github/workflows/release.yml`: a `linux` job (ubuntu-22.04, `cargo build --release --locked -p hive`) hands the binary to a `windows` job (static CRT) that runs `tauri-action` with `src-tauri/tauri.release.conf.json` (NSIS only, bundles `resources/hive`; kept out of `tauri.conf.json` because `tauri-build` requires resources to exist). The updater public key and endpoint (`releases/latest/download/latest.json`, passive install) are in `tauri.conf.json`; the private key was generated without a password at `~/.tauri/hive.key` (outside the repo) and goes into the `TAURI_SIGNING_PRIVATE_KEY` secret. `scripts/release.sh` bumps the app and `hive` versions (hive-protocol keeps its own), commits and tags. Not run: the workflow only runs on GitHub.* A tag `v*` builds the Linux `hive` (ubuntu-22.04) and the Windows NSIS installer with the updater artifacts (`tauri-action`), and publishes them with `latest.json` in a GitHub release; `scripts/release.sh <version>` bumps every manifest, commits and tags (the human pushes).
- [x] **4.18 Bundled service** (human decision, 2026-09-24: **replaces #29**, the human updates `docs/hive.md`). `task/4.18-bundled-service` — *`bridge_command` always passes `$1` = `HIVE_BRIDGE` and `$2` = the bundled `hive` (the app's resource `hive`, only when the file exists), both possibly empty. The script runs the override, else copies the bundled file (`wslpath -u`, verbatim `\\?\` prefix dropped) to `${XDG_DATA_HOME:-~/.local/share}/hive/bin/hive` via `hive.new` + rename when `cmp` says it differs and runs it, else `~/.cargo/bin/hive`. `main.rs` builds `Hive` in `setup` to resolve the resource. The version-mismatch dialog now says to stop the old service (`pkill`) and mentions `cargo install` for development builds. Tested with a fake `wslpath` in a temporary `HOME`; not yet run from a real installer (checkpoint 4b).* The installer carries the Linux `hive`; the bridge script copies it to `<data-dir>/hive/bin/hive` when it differs from the installed one and runs that copy. `HIVE_BRIDGE` still overrides it for development. One WSL distribution only (the default one).
- [x] **4.19 Update button** (human request, 2026-09-24). `task/4.19-update-button` — *`tauri-plugin-updater` (no frontend package; `tauri-plugin-process` not needed). Human decisions: `deny.toml` allows ISC (`ring`, `rustls-webpki`) and CDLA-Permissive-2.0 (`webpki-root-certs`), pulled in by the updater's HTTPS. Commands `check_update` / `install_update` (sync, spawned) answer on the UI channel: `update_available {version}` only when a newer release exists (no answer offline or on a broken manifest), `update_failed {error}`. `main.rs` calls `check_update` after `connect`; the update found is kept in `Link`; a successful install calls the restart `main.rs` gives (`Hive::with_restart` → `request_restart`, so the exit events end the connection). Title bar: "Update to vX" (Phosphor `ArrowCircleUp`, accent) left of the bell, "Updating…" and disabled while installing; with agents at risk it asks first through `CloseAppDialog updating` ("Update Hive?", "Update and restart"); a failure goes to the status bar notice and re-enables the button. Mock: `?mock=update`. Tested against a local HTTP stand-in for GitHub (newer/older/broken manifest, unsigned installer rejected). Found meanwhile: `sessions_of_followed_projects_are_listed_newest_first_and_deleted` failed once under `llvm-cov` load (likely equal mtimes), passed alone 5/5; `scripts/gates.sh`'s watchdog (`pgrep -f`) never ends while another shell's command line holds "cargo mutants" (two stale 4.10 shells did).* On start the app asks GitHub for a newer release (`tauri-plugin-updater`); when there is one, the title bar shows "Update to vX" left of the bell; a click installs it and restarts the app, asking first when agents would end.
- [x] **4.20 Terminal line height** (human request, 2026-09-24: "too tall"). `task/4.20-terminal-line-height` — *xterm `lineHeight` 1.2 (about 16 px rows at 13 px) instead of 19/13 (19 px). Frontend change only; not seen in the real app yet.*
- [x] **4.21 Side panel open by default** (human request, 2026-09-24). `task/4.21-right-panel-open` — *`rightPanel` starts as `"files"`; Ctrl+Shift+B and the toggle still close it. Not remembered across restarts.*
- [x] **4.22 Product name "Hive"** (human request, 2026-09-24). `task/4.22-product-name` — *`productName` is `Hive`, so the Start menu and desktop shortcuts, the installer (`Hive_<version>_x64-setup.exe`) and the window process read "Hive". Windows paths ignore case, so an update installs over the old `hive` folder. Not released yet (human: next release).*

**⏸ Checkpoint 4b — first releases.** The human adds the signing secrets, pushes `v0.1.0`, installs it, pushes `v0.1.1` and updates through the button.

**⏸ Checkpoint 4 — v1 complete.**

---

## Stage 5 — Folder browser and macOS (v0.1.2)

Human decisions (2026-09-24): Hive also runs natively on macOS, Apple Silicon only, ad-hoc signed (no Apple Developer account); terminals there open the user's login shell (`$SHELL`). **This adds a platform to `docs/hive.md` (Windows + WSL): the human updates it.** Plan: `~/.claude/plans/vamos-seguir-com-a-warm-pine.md`.

- [x] **5.0 Folder browser in Add project.** `task/5.0-folder-browser` — *defaults pending human review: `hive::dirs` answers `list_dirs` with the typed text as is (`path` echoes it, the home folder with a trailing separator for an empty one) and `windows` echoed so a late answer from the other side is ignored; without a trailing separator the folder holding the last name is listed; `linux_path` converts the whole typed text (`wslpath -u`) and is what "Add project" sends, so Add is enabled only once the answer for the current text has arrived; links to folders are listed, sorted ignoring case; the Windows home comes from `cmd.exe /c echo %USERPROFILE%` on each empty request (no cache); relative paths are refused ("type a full path"), paths over 4096 bytes too. Dialog: the filter keeps names containing the text after the last separator (any case); ↑ shows the parent path; the field fills with the home folder on open and on switching sides, not after the user clears it; folder rows are out of the tab order (typing is the keyboard path); the choice is kept as `hive.folderSide` (WSL by default). Mock: `MOCK_FOLDERS` and a fourth repo on the Windows side, `site` (`C:\Users\user\source\site`).* the service lists subfolders (`list_dirs {path, windows}` → `dirs {path, linux_path, parent, dirs[{name, git}], error}`; home when empty; no hidden ones; at most 1000). The dialog shows `[Windows | WSL] [path]` on Windows (the select only where both exist; none on macOS) with the typed folder's subfolders under the field: click to enter, ↑ to go up, a git icon on repositories; Windows paths are converted by the service (`wslpath`). A note under the field: Windows folders are slower and do not update live.
- [x] **5.1 Portable service.** `task/5.1-portable-service` — *`notify` 8.2 (inotify/FSEvents) replaces raw inotify, same `files::Watcher` API; reads are dropped in the callback, a bounded queue of 4096. `procs::Source::{System, Dir}`: `/proc` on Linux, `libproc` + `getsid` on macOS (libproc's `pidcwd` is a stub there: own `proc_vnodepathinfo` binding). `hive daemon` calls `setsid()` itself (no `setsid --fork`, no `unsafe`). macOS: `file::windows` returns the native path, macOS "runs as a program" list. Terminals: Linux unchanged (fish `-C`); macOS `$SHELL` (else `/bin/zsh`) as a login shell via `terminal::login` (portable, tested on Linux incl. a real bash run): fish `-l -C`, zsh Hive `ZDOTDIR` (`<data>/hive/shell`, its `.zlogin` prepends last), bash `--rcfile` reading `/etc/profile` + first login profile, others `-l` with `PATH`. All macOS-only glue in `crates/hive/src/macos.rs`, excluded from Linux coverage/mutants (human, 2026-09-24; macOS CI in 5.3 tests it). nix: `inotify` feature dropped, `process` added. Open: a native `claude` on macOS may not show comm `claude` (argv[0] via `KERN_PROCARGS2` would fix it); macOS integration tests need fish installed (`SHELL=fish`).* `setsid --fork` → in-process `setsid`; inotify → `notify` crate; a macOS process listing (`libproc`) behind `procs`; no `wslpath` on macOS and a macOS "runs as a program" list; terminals open `$SHELL` as a login shell with Hive's bin dir first (fish `-C`, zsh `ZDOTDIR`, bash `--rcfile`). Linux behaviour and coverage unchanged.
- [x] **5.2 macOS app.** `task/5.2-macos-app` — *`bridge_command(macos, …)` (main.rs passes `cfg!(target_os = "macos")`, so it is tested on Linux); `src-tauri/tauri.macos.conf.json` repeats the whole window entry (Tauri merges arrays by replacing them); `isMac`/`commandKey`/`keyText` in `src/window.ts` read `navigator.userAgent`; tests call `asMac()` (`test/mac.ts`), reset after each test in `test/setup.ts`. Traffic lights keep their default position (the bar is 34px; `trafficLightPosition` is the knob if they sit too high). Not checked on a real Mac yet: whether the default macOS Edit menu takes Cmd+V before the terminal.* `bridge_command` runs the bridge script with `/bin/sh` on macOS (POSIX bundled path, quarantine removed from the copy); native traffic lights (`titleBarStyle: Overlay`); platform wording (no WSL/Explorer on macOS); Cmd instead of Ctrl for app shortcuts and terminal copy/paste on macOS.
- [x] **5.3 macOS build and release.** `task/5.3-macos-release` — *`src-tauri/tauri.release.macos.conf.json` (release-only, since `tauri.macos.conf.json` is merged in dev too): app + dmg, bundled `resources/hive`, ad-hoc signing (`-`), macOS 12+. `release.yml`: a `macos` job on `macos-14` builds `hive` and runs `tauri-action`; it `needs: windows` because tauri-action reads, extends and re-uploads `latest.json` (parallel jobs would drop a platform). `macos.yml` (push to main, workflow_dispatch): clippy, `cargo llvm-cov` at 100% with `macos.rs` included, `cargo mutants --no-config -f crates/hive/src/macos.rs`, `bun test`, and a `bundle` job that builds the release `.dmg`/`.app.tar.gz` without publishing (artifact `hive-macos`, 7 days). Actions on Node 24 (checkout v6, upload v6, download v7). The first Mac runs fixed tests that assumed Linux (case-sensitive and non-UTF-8 names, `/var` → `/private/var`, BSD `ps`/`wc`, macOS's dash exec-ing its last command, fish 4 waiting for the terminal's DA1 answer, FSEvents reporting writes made just before a watch, a store leak between test files) and dropped `macos.rs`'s zombie check (libproc already fails for zombies). Found: a native `claude` (a symlink to `versions/<x.y.z>`) shows as `<x.y.z>` in the kernel's process name on macOS, so detection by name misses it.* `tauri.macos.conf.json` (app + dmg, bundled `hive`, ad-hoc signing, macOS 12+); a `macos-14` job in the release workflow (adds `darwin-aarch64` to `latest.json`); a `macos.yml` CI running the Rust and frontend tests on macOS (the only place the macOS-only code is tested); actions bumped to Node 24.
- [x] **5.4 Worktree above its subagent.** `task/5.4-worktree-above-subagent` — *frontend only (the service already sends each subagent's worktree): `SubagentNode` renders the own worktree (`.own-worktree`, 22px, at the subagent indent 56px) with the subagent in a nested `ul.owned` at 72px; the agent's tree line runs on past both; until `projects` lists the worktree the subagent stays in place.* in the sidebar a subagent's own worktree becomes the parent row and the subagent is indented under it (Project → Worktree → Agent at every level), instead of the worktree under the subagent. Human decision 2026-09-24; `docs/hive.md` 3.4 records it (list item 3 and #22).
- [x] **5.5 Session card uses the full width.** `task/5.5-session-card-width` — *CSS only: `.session-main` pads 12px on both sides; on hover/focus-within the title row alone gets a 28px right padding for the `⋯` button (the title already keeps one line with an ellipsis).* in the Sessions view the card reserves a right gutter for the `⋯` actions button (`.session-main` `padding-right: 40px`) even though the button only shows on hover/focus and only beside the title. Drop the gutter: title, last message and meta use the whole width; on hover/focus only the title row makes room for the button, and the title stays on one line with an ellipsis. Human request 2026-09-24.
- [x] **5.6 No alert for the agent you are watching.** `task/5.6-focused-agent-quiet` — *defaults pending human review: the tone still plays (the decision names only the OS notification and the counter); the exception covers only finishing (🔵/🟣 → 🟠, silence included), not 🟡/🔴; a seen agent stays not pending when you look away, until its state changes; an agent already pending when you look at it stays pending; "in view" is the shown terminal tab, not while a file is shown. Protocol: `view {terminal, focused}` on channel 0, sent on change and after `welcome`; `agent_state.pending` is false for a seen agent and the app notifies only when it is true.* when an agent finishes (🔵/🟣 → 🟠) while the Hive window has focus and that agent's terminal is the one in view, there is no OS notification and it does not raise the pending counter at the top (it is already seen). With the window unfocused, or another agent in view, everything behaves as today. The app tells the service which agent is in view and whether the window has focus; the service keeps deciding `pending` (#37). Human decision 2026-09-24; `docs/hive.md` 3.4 records it (list item 5). when an agent finishes (🔵/🟣 → 🟠) while the Hive window has focus and that agent's terminal is the one in view, there is no OS notification and it does not raise the pending counter at the top (it is already seen). With the window unfocused, or another agent in view, everything behaves as today. The app tells the service which agent is in view and whether the window has focus; the service keeps deciding `pending` (#37). Human decision 2026-09-24; `docs/hive.md` 3.4 records it (list item 5).
- [x] **5.7 Quieter resize edges, only the dragged one lit.** `task/5.7-resize-edges` — *defaults pending human review: the lit edge is a 2px `--focus` line centred in the 6px hit area at 70% opacity (hover, keyboard focus and drag alike); `body[data-resizing]` now names the dragged side (`sidebar` / `panel`), so only that edge stays lit.* the panels' resize edges (`.resize-handle`, 6px of solid `--focus`) are slightly more discreet (thinner visible line and/or softer colour, same 6px hit area). While dragging, only the edge being dragged is lit: today `body[data-resizing] .resize-handle` lights both the sidebar and the right-panel edges. Human request 2026-09-24.
- [x] **5.8 No project name in the right panel header.** `task/5.8-panel-header-branch-only` — *`WorktreeInfo` takes only the worktree; its name now ellipsizes on one line (the project's span and style are gone).* the right panel's worktree header (`WorktreeInfo` in `src/shell/RightPanel.tsx`) shows `⑂ main  hive`; drop the project name (redundant: the panel always shows the worktree of the project you are in) and keep only the branch icon and worktree name. Human request 2026-09-24.

- [x] **5.9 E2E specs catch up; flaky tests.** `task/5.9-e2e-and-flaky` — *specs only, no product change: the side panel starts open (shell, connection, shortcuts, files, editing, reference); the Add project field is "Folder", Enter waits for Add to be enabled, and the spec walks the folder list once; tab lists are scoped to "Open terminals and files" (the panel has its own); per-worktree tabs (4.7): a file's tab leaves with its worktree, and a worktree with no terminal answers "No terminal open" to Send to terminal; the mock echoes `claude` before its first prompt; state icons are measured by their layout box (computed width/height), and the collapsed rollup is 14px like the rest. Rust: bridge test scripts are written by a child `sh` (no write fd of the test process to leak into a concurrent fork → no ETXTBSY); `a_second_app_is_rejected` waits for the first app's `projects` answer (`Welcome` comes before the daemon takes the app slot); the sessions test sets mtimes explicitly.* 13 Playwright specs fail on main (found by 5.2 and 5.4): specs not updated for the side panel open by default (4.21), the Add project "Folder" field (5.0) and the spinning state icon measured mid-rotation (`states.e2e.ts`). Also three flaky Rust tests: `src-tauri` `the_bridge_script_prefers_the_override_then_cargo_install` (ETXTBSY running a script right after writing it), integration `daemon::a_second_app_is_rejected` (timeout under load), `sessions_of_followed_projects_are_listed_newest_first_and_deleted` (order under load). Fix the causes, never loosen assertions.

- [x] **5.10 Subagents waiting on a background command stay listed.** `task/5.10-subagent-background-wait` — *found in Claude Code 2.1.282 (read from its bundle, not recorded live): `Stop`/`SubagentStop` payloads carry `background_tasks` (the session's running/pending backgrounded tasks: `id`, `type`, `status`, `description`; shared by the whole session, no owner), and the `PostToolUse` `tool_response` names the task started (`backgroundTaskId` Bash, `taskId` Monitor, `agentId` Agent; seen in the transcripts' `toolUseResult`). A woken subagent fires `SubagentStart` again. Defaults pending human review: `states::Agent` remembers each subagent's launched ids (at most 32, oldest dropped); on its `SubagentStop` it stays listed as working while one of them is in `background_tasks`, silence does not turn it 🟠; any later `Stop`/`SubagentStop` list without its launches, its own worktree's `WorktreeRemove`, its `SessionEnd` or the agent's end removes it; a stop without `background_tasks` (older Claude Code) removes it as before. Not yet confirmed with `hive hook --record` on a real session (human).* bug found 2026-09-24 in the Stage 5 run: Claude Code listed 6 live background subagents, the sidebar showed 2. Cause (from the subagent transcripts in `~/.claude/projects/…/083ccf7c…/subagents/`): a subagent that starts a command with `run_in_background` and then ends its turn to wait for it (`stop_reason: end_turn`, e.g. 5.1 "Gates running…; waiting.", 5.4, 5.3) fires `SubagentStop`, and `Agent::apply` (`crates/hive/src/states.rs`) removes it on `SubagentStop`; it only comes back on its next tool event, when the background-task notification wakes it. The 2 shown (5.6, 5.8) were inside a foreground `Bash` call. Their worktrees stayed as bare rows under the project. Fix in the service: a `SubagentStop` while that subagent still has a background launch it has not been woken for keeps it listed (state stays 🔵 working, the worktree stays its own); it goes away on a `SubagentStop` with nothing pending, the parent's `SessionEnd`, or its worktree's `WorktreeRemove`. First record the real payloads (`hive hook --record`, per the spike tooling; never run the real `claude` yourself, ask the human) to confirm which field marks a background launch (`tool_input.run_in_background` on `PreToolUse Bash`; also `Monitor` and background `Agent` calls) and whether `SubagentStop` carries anything better. The `docs/hive.md` state table (🟣 via `SubagentStart`/`SubagentStop`) is the human's to update.

- [ ] **5.11 Gates in CI.** `task/5.11-gates-in-ci` — human decision 2026-09-24: the heavy gates run on GitHub Actions instead of the human's machine (the repo is public, standard runners are free). A `ci.yml` on every push of `task/*` and `main` runs every Rust gate (fmt, clippy, test, check --locked, deny, machete, llvm-cov 100%, mutants on the branch's diff against main), the frontend gates and the Playwright e2e; `macos.yml` runs on `task/*` too. Agents push their task branch (allowed from now on; never `main`, never tags outside a release), wait for CI (`gh run watch`) and fix what fails; locally they run only fast checks. A task merges into `main` only with green CI. The human authorised the agent to update `CLAUDE.md` (gates section, push rule) and `.claude/skills/stage/` accordingly.
- [ ] **5.12 Native `claude` detected on macOS.** `task/5.12-macos-claude-name` — found by 5.3 on the Mac runner: a native `claude` is a symlink to `versions/<x.y.z>` and macOS names the process after the resolved file, so `procs::claude_cwds` / `watch::claude_sessions` miss it (no unhooked warning, sessions not marked running). Read argv[0] through the `KERN_PROCARGS2` sysctl; needs `libc` as a direct dependency (ask the human first).
- [ ] **5.13 File tree icons.** `task/5.13-file-tree-icons` — human request 2026-09-24: `bun add @react-symbols/icons` (human-approved) and use its file and folder icons for every file type and folder it covers, **monochrome** (one colour from the theme, e.g. `currentColor` in the muted text colour), **only in the file tree** (right panel Files/Diff lists), nowhere else. Simple and clean; the default icon for unknown files/folders.
**⏸ Checkpoint 5 — v0.1.2.** The human runs `scripts/release.sh 0.1.2`, pushes, installs the `.dmg` on the Mac, allows it in Privacy & Security, opens a project, a terminal and `claude`; Windows updates through the button.

---

## Not now (Fase 2)

Kanban screen, rich interactions (permissions and choices answered in the app), live edit view, session history, GitHub CLI integration. Do not build any of it during v1.

## Open items for the human

- Export PNG screenshots of prototype screens 1a–1g into `docs/prototype/stage-1/` before Stage 1 (Ponto em aberto 13).
