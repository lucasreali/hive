[package]
name = "hive-protocol"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
bytes = "1.12.1"
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
thiserror = "2.0.20"
tokio-util = { version = "0.7.19", features = ["codec"] }

[lints]
workspace = true
